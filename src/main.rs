use rand::Rng;

fn main() {
    
}

fn roll_stat_block() -> Vec<i32> {

}

fn roll_stat() -> i32 {
    let rolls: Vec<i32> = Vec::new();

    loop {
        if (rolls.len() == 4) {
            
        }
    }
}